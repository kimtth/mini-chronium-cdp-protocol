[project]
name = "mini-chronium-mojo-ipc-cdp-protocol"
version = "0.1.0"
description = "Real Chrome DevTools Protocol example using uv"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "websockets>=16.0",
]
